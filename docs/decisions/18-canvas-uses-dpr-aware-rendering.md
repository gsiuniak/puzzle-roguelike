# Decision #18 — Canvas uses DPR-aware rendering

> Extracted verbatim from CLAUDE.md §7 (2026-07-14 split). Historical record — reflects the design at time of writing.

**Canvas uses DPR-aware rendering** — all layout is in CSS pixels; context is pre-scaled.
